# Phase 1, Plan 01: Environment & Scaffolding

**Plan ID:** 01-01
**Wave:** 1
**Objective:** Initialize the modular project layout, setup the Python virtual environment, and define dependencies.

## Requirements Addressed
- **AUST-01**: Standard system microphone recording (prerequisite: environment setup).

## Implementation Details
- **Structure:** Create `voxpolish/core/` and its sub-modules (`audio.py`, `stt.py`, `grammar.py`).
- **Environment:** Initialize `venv` in the project root.
- **Dependencies:** List common core libraries: `sounddevice`, `numpy`, `webrtcvad-wheels`.

## Tasks
```xml
<task id="TASK-01-01-01" wave="1">
  <objective>Create the modular project structure and __init__.py files</objective>
  <read_first>
    <file>ROADMAP.md</file>
  </read_first>
  <action>
    Create the directory structure:
    - voxpolish/
    - voxpolish/core/
    Create empty __init__.py files in both.
    Create stubs for:
    - voxpolish/core/audio.py
    - voxpolish/core/stt.py
    - voxpolish/core/grammar.py
    - main.py (entry point)
  </action>
  <acceptance_criteria>
    - Directory voxpolish/core/ exists.
    - All stub files exist.
  </acceptance_criteria>
</task>

<task id="TASK-01-01-02" wave="1">
  <objective>Create the requirements.txt file with core dependencies</objective>
  <read_first>
    <file>.planning/phases/01-project-setup-base-audio/01-RESEARCH.md</file>
  </read_first>
  <action>
    Write requirements.txt including:
    - sounddevice
    - numpy
    - webrtcvad-wheels
    - google-generativeai (for Phase 2+)
    - python-dotenv
  </action>
  <acceptance_criteria>
    - requirements.txt contains sounddevice, numpy, webrtcvad-wheels.
  </acceptance_criteria>
</task>

<task id="TASK-01-01-03" wave="1">
  <objective>Initialize the venv and provide install instructions</objective>
  <read_first>
    <file>requirements.txt</file>
  </read_first>
  <action>
    Initialize the Python virtual environment:
    python -m venv venv
    Provide a shell command file for installation on Windows.
  </action>
  <acceptance_criteria>
    - venv directory exists.
  </acceptance_criteria>
</task>
```

## Verification
- Running `python -c "import voxpolish.core.audio"` results in no errors.

---
*Plan 01-01 for Phase 1.*
